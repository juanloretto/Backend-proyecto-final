/* const { model, Schema } = require('mongoose')
const AssetsSchema = Schema({
    titulo: {
        type: String,       
    },
    imagen: {
        type: Image,
        required: [true, "La imagen es obligatoria"]
    }
}) */